# Never commit real secrets. Copy to .env locally.
#
# For local docker-compose (default), you don't need to set this.
# For MongoDB Atlas (your cluster), set:
# MONGO_URI=mongodb+srv://<user>:<password>@<cluster-host>/noshow_iq?retryWrites=true&w=majority&appName=MLops-Mid
MONGO_URI=

